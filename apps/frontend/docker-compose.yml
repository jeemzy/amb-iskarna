services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://192.168.88.130:8765}
    container_name: amb-iskarna-frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
